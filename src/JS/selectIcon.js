import weatherIcon from './weatherIcon';

const {
  cloud,
  rainy,
  heavyShower,
  sunny,
  thunder,
  snow,
  clearSky,
} = weatherIcon();

const selectImage = (obj) => {
  switch (obj) {
    case 'Thunderstorm':
      
      break;
  
    default:
      break;
  }
}